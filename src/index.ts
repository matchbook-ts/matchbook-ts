import { otherwise, match, strike, pattern, unwrap } from './functions';

// prettier-ignore
export {
    strike,
    pattern,
    pattern as book,
    match,
    match as m,
    otherwise as _,
    otherwise as rest,
    otherwise,
    unwrap,
};
