import {otherwise, match, strike, pattern, unwrap} from './public';

// prettier-ignore
export {
    strike,
    pattern,
    pattern as book,
    match,
    match as m,
    match as arm,
    otherwise,
    otherwise as _,
    otherwise as rest,
    unwrap,
};
